
const Loader = () => {
    return (
        <div className="h-screen">
            <span className="loading loading-spinner loading-lg"></span>
        </div>
    );
};

export default Loader;